export class Fish {
  public id: number;
  public name: string;
  public quantity: number;

  constructor (id: number, name: string, quantity: number) {
    this.id = id
    this.name = name
    this.quantity = quantity
  }
}
